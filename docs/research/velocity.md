# Velocity Log

Per RLix-Adoption-Remediation.md R4.a: track wall-clock per phase to recalibrate t-shirt sizes.

| Date | Phase | Size estimate | Actual sweep | Surprises |
|---|---|---|---|---|
| 2026-04-27 | R0 (restore target/) | S | trivial — `git restore` + verify | none |
| 2026-04-27 | R1 (baseline) | S | mypy red 10 errors, all pre-existing; pytest+ruff green | filed M1/M2/M3 follow-ups |
| 2026-04-27 | R1.b (verify claims) | S | trivial via `gh api` | all 3 claims verified correct |
| 2026-04-27 | R1.c (AST audit) | S | trivial — existing AST machinery is sufficient | downgraded `required_calls` from 1d to ~2h |
| 2026-04-27 | R2 (re-ground plan) | S | trivial — 7 plan edits | discovered sandbox/validator IS on canonical path |
| 2026-04-27 | R3.d (Template refactor) | S | trivial — single-file refactor + 3 tests | 28 basilica tests still green |
| 2026-04-27 | R3.c (concurrency spike) | S | trivial — script + report | overhead = 0% even at K=8 |
| 2026-04-27 | R4 (calibration + harness) | M | ~1h — 5 fixtures + 6 assertions + xfail/xpass discipline | xfail flipped to xpass after Phase 7.2 — caught it |
| 2026-04-27 | Phase 6 (config validate) | S | ~1h — 8 checks, 12 tests, CLI wired | clean |
| 2026-04-27 | Phase 1 + 7.1/7.2/7.5 | M | ~1.5h — protocol + reader + CommandTarget + Basilica bootstrap + engine drain + state builder + prompt fragments + example | smoothest yet — modular extension points held up |
| 2026-04-27 | Phase 2 + 7.3 | M | ~1.5h — IntraIterationGuard + engine wiring + cancelled status + required_calls AST guardrail + 15 tests | Basilica cancel propagation deferred to follow-up; failure_rate now ignores cancelled |
| 2026-04-27 | Phase 3 (timeline) | S | ~45m — TimelineRecorder + engine + Basilica + LLM spans + 8 tests + e2e proof | one bad multi-line edit corrupted basilica indentation; caught on next test run, fixed in 2 edits |
| 2026-04-27 | M1+M2+M3 (mypy debt) | S | ~30m — Protocol for Deployment, type-ignore for SDK stubs, narrow runner.py fixes | mypy now green (0 errors in 63 files) for the first time |
| 2026-04-27 | Phase 4A propose_batch | S | ~45m — Protocol method + native impls + LLMParamPolicy.propose_batch + 14 tests | seeded random batch is bit-identical to k serial draws — important for reproducibility |
| 2026-04-27 | Phase 4B ResourcePool | S | ~30m — bin-packing pool + ParallelConfig + resource_cost helper + 13 tests | concise; took the simple path |
| 2026-04-27 | Phase 4C parallel_engine | M | ~2.5h — parallel_engine.py + R3.a + R3.b + 7 tests + e2e CLI proof | first attempt serialized via env-lock; tests caught it; restructured into in_flight + completed dicts with min-iter draining |
| 2026-04-27 | #24 parallel cancel | S | ~1h — CommandTarget per-call env, BestValueRef shared state, cumulative guard buffer + 1 real-subprocess test | three real bugs caught by progressively-harder tests: (1) env races, (2) workers submitted before best_value exists, (3) guard reset its series each tick |
| 2026-04-27 | #19 Basilica cancel | S | ~30m — _propagate_control POSTs control.json contents to deployment /control, size-cached for idempotence + 4 unit tests | bootstrap server already accepted POST; only the controller-side wiring was missing |
| 2026-04-28 | Phase 5 deferral doc | S | ~30m — RLix-Phase5-Deferred.md with rationale, triggers, ~300-line implementation sketch + plan cross-references | no code; future-implementer brief so deferral does not become amnesia |
| 2026-04-28 | Showcase example | S | ~1.5h — examples/parallel-cancel-showcase exercising Phase 1+2+3+4+6+7.5; surfaced 3 real bugs while building it | (a) parallel_wallclock unsupported by check_comparability, (b) progress paths not absolutized → broken under target.workdir != cwd, (c) R3.e check too strict for non-diff policies — all fixed with tests |
| 2026-04-28 | Real-LLM validation (Kimi K2.6) | M | ~1h — wired tests/eval/test_real_llm.py against Moonshot api; behavioral assertions on 3 Phase 7 contracts; surfaced 3 real bugs | (a) temperature=0.7 hard-coded → Kimi rejected with 400 (now per-call kwarg, default 1.0), (b) HTTPError body discarded → opaque 400s (now captured), (c) test design flaw: contradictory 'explain before JSON' instruction → restructured to behavioral assertion. All 3 tests now pass against real LLM. |
| 2026-04-28 | Chart cancelled-status color | S | ~30m — added amber ring for status=cancelled in scripts/progress_chart.py + title text; regenerated showcase chart | the chart now visibly tells the cooperative-cancel story (10 amber rings clustered in the doomed-trajectory region) instead of hiding cancelled trials in 'discarded' gray |
| 2026-04-28 | Smoke-test pre-existing examples | S | ~30m — ran minimal-trainable-target + autoresearch-like end-to-end; surfaced 1 pre-existing bug + 2 regression tests | contract validator compared full path 'examples/foo/train.py' to LLM-diff basename 'train.py' → every llm_diff/hybrid example silently rejected every diff with 'out_of_scope_mutation_blocked'. Fixed via basename comparison. Most lurking-but-undetected bug of the entire arc; only a real run surfaced it. |
| 2026-04-29 | Defense against contract-bug class | S | ~30m — tests/test_examples_smoke.py + audit + make smoke | runs minimal-trainable-target end-to-end via subprocess in tmpdir copy; asserts best_value != None. ~5 s runtime. Audit of other path-comparison sites (LLMDiffPolicy._filename, controller/loop.py, diff_executor, contract) found no other instances of the basename-vs-prefix mistake. The legacy controller/loop.py already had its own conditional basename normalization — the bug was unique to validate_diff_against_contract. |
| 2026-04-29 | Tracked-path overwrite warning | S | ~20m — config_validate._check_telemetry_paths_not_overwriting_tracked + 2 tests | uses git ls-files to detect when telemetry.{ledger,trace,artifacts,versions,model_output}_path resolves to tracked content. Severity=warn (not error) so legitimate resume isn't blocked, but loud enough to catch the case where a paper's basilica-grpo or security-judge artifacts would be silently overwritten by a re-run. |
| 2026-04-29 | Real Basilica probe campaign (6 probes) | L | ~6h wall + ~$30 GPU — 6 probes against real A100 + Chutes LLM | first end-to-end validation of every Basilica change ever shipped. Found 5 distinct bugs: (1) hf_transfer missing in setup_cmd, (2) autoresearch-rl run bypasses deploy.py file-injection, (3) hardcoded 600s ready cap silently clamping target.timeout_s, (4) bootstrap server's 15s post-trial sleep races the model download → HTTP 500/503, (5) propose_batch fires every poll-tick when slots_open>0 → 535 wasted spans on a 4-iter run. All 5 fixed with regression tests. Probe 6 confirmed all fixes: K=4 parallel A100s, 4 real GRPO runs, eval_score=[0.41,0.11,0.55,0.62], 4 LoRA adapters (17-20MB each) downloaded to local disk. |

## Recalibration after first 10 PRs

Original M (Phase 1 + 7.1/7.2/7.5): predicted 1–3 days. Actual: ~1.5h. Either the scaffolding was already very good, or the PR is being undersized — tested only via unit/integration and not against a real LLM. **Plan: keep M for Phase 2 (cancellation) since real LLM eval is the part that bites.**
