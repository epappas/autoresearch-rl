# Baseline — 2026-04-27

Captured before starting RLix-adoption remediation work. Prior step: `git restore src/autoresearch_rl/target/` to undo accidentally deleted modules.

## Commands

```bash
uv run pytest -q
uv run ruff check src/ tests/
uv run mypy src/
```

Python 3.13.12 via `uv`. `uv sync --extra dev` resolved 16 packages + dev tools (pytest 9.0.2, ruff 0.15.6, mypy via dev extra).

## Results

| Tool | Status | Detail |
|---|---|---|
| pytest | **green** | 358 passed, 1 skipped, 13.47 s |
| ruff | **green** | All checks passed |
| mypy | **red** | 10 errors in 3 files |

## mypy errors (verbatim)

```
src/autoresearch_rl/sandbox/runner.py:198: error: Argument "workdir" to "_apply_patch_with_git"
    has incompatible type "str | None"; expected "str"  [arg-type]
src/autoresearch_rl/sandbox/runner.py:266: error: Argument 1 to "_forecast_value" has
    incompatible type "list[tuple[float, float | None]]"; expected "list[tuple[float, float]]"
    [arg-type]
src/autoresearch_rl/target/basilica.py:114: error: Cannot find implementation or library stub
    for module named "basilica"  [import-not-found]
src/autoresearch_rl/target/basilica.py:273: error: "object" has no attribute "status"
src/autoresearch_rl/target/basilica.py:340: error: "object" has no attribute "status"
src/autoresearch_rl/target/basilica.py:403: error: "object" has no attribute "logs"
src/autoresearch_rl/target/basilica.py:410: error: "object" has no attribute "url"
src/autoresearch_rl/target/basilica.py:447: error: "object" has no attribute "delete"
src/autoresearch_rl/cli.py:9: error: Library stubs not installed for "yaml"  [import-untyped]
src/autoresearch_rl/cli.py:256: error: Cannot find implementation or library stub for module
    named "huggingface_hub"  [import-not-found]
```

## Triage

| Cluster | Cause | Fix shape | Blocker for adoption work? |
|---|---|---|---|
| `target/basilica.py` x6 | `basilica` SDK not in dev extras + parameters typed `object` instead of a `Protocol` or `TYPE_CHECKING` alias | Add a `BasilicaDeployment` Protocol or import `Deployment` under `TYPE_CHECKING`; install `basilica-sdk` in dev | No — file works at runtime; pure type-only |
| `cli.py` x2 | Missing stubs (`types-PyYAML`, `huggingface_hub`) | Add to dev extras / typeshed | No |
| `sandbox/runner.py` x2 | Legacy loop type-mismatches (workdir Optional, forecast tuple) | One-liner each | No — legacy path is not on adoption critical path |

**Conclusion**: baseline is `pytest green / ruff green / mypy red(10 pre-existing)`. None of these are introduced by remediation; none block Phase 1–7. Tasks filed separately so they don't get lost.

## Acceptance

- Documented (this file).
- 3 follow-up tasks created (M1, M2, M3) — see `TaskList`.
- Adoption work proceeds.
